[pytest]
markers =
    integration: integration tests with real dependencies
    docker: requires docker compose stack
    pnet: requires access to PNET
    slow: slow-running tests
    gunicorn_only: must run inside the web container (needs live gunicorn process)
